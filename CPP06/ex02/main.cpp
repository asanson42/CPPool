#include "Class.hpp"

int	main( void ) {

	Base*	base;

	base = generate();
	identify(*base);
	delete base;
	base = NULL;

	base = generate();
	identify(*base);
	delete base;
	base = NULL;

	base = generate();
	identify(*base);
	delete base;
	base = NULL;

	base = generate();
	identify(*base);
	delete base;
	base = NULL;

	base = generate();
	identify(*base);
	delete base;
	base = NULL;

	base = generate();
	identify(*base);
	delete base;
	base = NULL;

	base = generate();
	identify(*base);
	delete base;
	base = NULL;

	base = generate();
	identify(*base);
	delete base;
	base = NULL;

	base = generate();
	identify(*base);
	delete base;
	base = NULL;

	std::cout << std::endl;

	base = generate();
	identify(base);
	delete base;
	base = NULL;

	base = generate();
	identify(base);
	delete base;
	base = NULL;

	base = generate();
	identify(base);
	delete base;
	base = NULL;

	base = generate();
	identify(base);
	delete base;
	base = NULL;

	base = generate();
	identify(base);
	delete base;
	base = NULL;

	base = generate();
	identify(base);
	delete base;
	base = NULL;

	base = generate();
	identify(base);
	delete base;
	base = NULL;

	base = generate();
	identify(base);
	delete base;
	base = NULL;

	base = generate();
	identify(base);
	delete base;
	base = NULL;
}
